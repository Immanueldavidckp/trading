<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="online.scratchforge.trade"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- targetSdk 34 on purpose. From 35 Android forces edge-to-edge, which
         draws the status bar and gesture pill over the page; a WebView does
         not feed those insets to CSS env(safe-area-inset-*), so the bottom
         tab bar ends up under the pill. Targeting 34 keeps the system
         insetting the window, which is exactly what this layout wants.
         Nothing here needs a 35+ API, and the app is sideloaded, so no Play
         Store target requirement applies. -->
    <uses-sdk android:minSdkVersion="23" android:targetSdkVersion="34" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:hardwareAccelerated="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="online.scratchforge.trade.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/app_name">

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- open trade.scratchforge.online links in the app -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="trade.scratchforge.online" />
            </intent-filter>
        </activity>

    </application>
</manifest>
